{
  "id": "pth-impacket",
  "name": "Impacket - Pass the Hash Execution",
  "command": "impacket-psexec <user>@<ip> -hashes :<nt_hash>",
  "description": "Executes commands on a remote host by authenticating with an NT hash (-hashes LM:NT, LM left blank). psexec gives a SYSTEM shell; wmiexec, atexec, and smbexec offer quieter execution channels using the same hash.",
  "platform": "linux",
  "requires": [
    "hash"
  ],
  "protocols": [
    "tcp"
  ],
  "tools": [
    "impacket"
  ],
  "tags": [
    "impacket",
    "pass-the-hash",
    "psexec",
    "wmiexec"
  ],
  "category": "Lateral Movement",
  "subcategory": "Pass the Hash",
  "certifications": [
    "CPTS",
    "OSCP"
  ],
  "primary_cert": "CPTS",
  "source": "CPTS Module 10: Password Attacks",
  "examples": [
    {
      "label": "PsExec with the NT hash",
      "command": "impacket-psexec administrator@10.129.201.126 -hashes :30B3783CE2ABF1AF70F77D0660CF3453"
    },
    {
      "label": "OSCP Ch15 — wmiexec PtH (quieter than psexec; no service/binary dropped on disk)",
      "command": "impacket-wmiexec -hashes 0:<nt_hash> <user>@<ip>"
    }
  ],
  "notes": "Alternatives: impacket-wmiexec, impacket-atexec, impacket-smbexec. impacket-wmiexec is quieter than psexec — it executes commands via WMI without dropping a service binary. Also covered in: OSCP PEN-200 Chapter 15 (Password Attacks).",
  "references": [
    {
      "title": "HTB Academy - Password Attacks",
      "url": "https://academy.hackthebox.com/module/details/147"
    },
    {
      "title": "Impacket",
      "url": "https://github.com/fortra/impacket"
    },
    {
      "title": "OffSec PEN-200 - Password Attacks",
      "url": "https://www.offsec.com/courses/pen-200/"
    }
  ],
  "recommended": [
    {
      "id": "pth-netexec",
      "note": "Authenticates to SMB with an NT hash across one host or a whole subnet to find where the credential is valid, then runs commands with -x.",
      "rel": "alternative"
    },
    {
      "id": "pth-evilwinrm",
      "note": "Opens an interactive WinRM (PowerShell) session authenticating with an NT hash via -H.",
      "rel": "alternative"
    },
    {
      "id": "secretsdump-ntds",
      "note": "Parses an exfiltrated NTDS.dit with the matching SYSTEM hive to dump every domain account's NT hash offline.",
      "rel": "prereq"
    }
  ],
  "opsec": "loud",
  "mitre": [
    "T1550.002"
  ],
  "exam": "exam-ok",
  "defense": {
    "why_it_works": "Impacket tools (psexec/wmiexec/smbexec) authenticate with an NTLM hash instead of a password (-hashes) to execute commands on a target.",
    "prerequisites": "A valid NTLM hash for an account with rights on the target and SMB/RPC reachable.",
    "impact": "Command execution on the target as that account - lateral movement without cracking the hash.",
    "detection": "[MITRE T1550.002] Event 4624 Logon Type 3 with Authentication Package NTLM and LogonProcessName NtLmSsp, with NO preceding interactive logon for that account; the same NTLM hash authenticating from an unusual source; 4672 for privileged PtH.",
    "artifacts": "An NTLM network logon (4624 type 3) from impacket -hashes; a session as the target user without their plaintext password.",
    "prevention": "Enable Credential Guard and disable NTLM where possible; [MITRE T1550.002 M1026 Privileged Account Management] never reuse local-admin passwords (LAPS) so one hash doesn't unlock the network; restrict lateral admin logons; monitor NTLM auth.",
    "evasion": "PtH uses valid material so no password failures occur - it blends with normal NTLM auth; using an allowed source host and common hours reduces suspicion.",
    "sources": [
      "HTB M10",
      "MITRE T1550.002"
    ],
    "misconfiguration": "NTLM authentication is enabled on the network and the same local administrator password (or domain credential NTLM hash) is valid across multiple machines. When LSASS holds an NTLM hash in memory (from an interactive logon), any process running as SYSTEM can extract it and authenticate to remote services without knowing the plaintext password.",
    "vulnerable_config": "# Machines with identical local admin passwords (the classic PtH enabler)\n# No credential guard, no Protected Users group\n# NTLM enabled (default on all Windows versions)\n\n# Confirm NTLM is negotiated (SMB example):\n# Network capture shows NTLMSSP_AUTH frames on port 445\n\n# Accounts NOT in Protected Users group (PtH works against these):\nGet-ADGroupMember 'Protected Users'  # should list all privileged accounts\n\n# Local admin SID S-1-5-21-...-500 (RID 500) allows remote PtH by default\n# Even with UAC RemoteRestrictions, RID-500 bypasses the filter",
    "secure_config": "# 1. Enable Credential Guard (blocks NTLM hash extraction from LSASS)\n# GPO: Computer Configuration > Admin Templates > System > Device Guard\n#   'Turn on Virtualization Based Security' = Enabled\n#   'Credential Guard Configuration' = Enabled with UEFI lock\n\n# 2. Add privileged accounts to Protected Users group (no NTLM, no RC4)\nAdd-ADGroupMember -Identity 'Protected Users' -Members 'Domain Admins'\n\n# 3. LAPS — unique random local admin passwords per machine\n# Eliminates lateral movement via shared local admin hash\nEnable-LapsADSchema\nSet-LAPSConfig -PolicyMode Automatic -PasswordLength 20 -PasswordAgeDays 30\n\n# 4. Block NTLM (requires AES Kerberos everywhere first)\n# GPO: Security Settings > Local Policies > Security Options\n#   'Network Security: Restrict NTLM: Incoming NTLM traffic' = Deny all\n\n# 5. Disable RID-500 remote admin:\n# HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Policies\\System\n#   LocalAccountTokenFilterPolicy = 0 (default)"
  }
}