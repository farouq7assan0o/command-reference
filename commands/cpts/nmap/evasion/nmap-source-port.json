{
  "type": "command",
  "platform": "linux",
  "requires": [
    "network-access"
  ],
  "protocols": [
    "tcp"
  ],
  "tools": [
    "nmap",
    "ncat"
  ],
  "tags": [
    "nmap",
    "source-port",
    "evasion"
  ],
  "category": "Enumeration",
  "subcategory": "Nmap - Firewall & IDS/IPS Evasion",
  "certifications": [
    "CPTS",
    "OSCP"
  ],
  "primary_cert": "CPTS",
  "source": "CPTS Module 03: Network Enumeration with Nmap",
  "examples": [
    {
      "label": "Baseline (filtered) without source port",
      "command": "sudo nmap 10.129.2.28 -p50000 -sS -Pn -n --disable-arp-ping --packet-trace"
    },
    {
      "label": "Connect via netcat from source port 53",
      "command": "ncat -nv --source-port 53 10.129.2.28 50000"
    }
  ],
  "notes": "Related evasion flags: -f (fragment), --mtu, --data-length, --dns-server <ns>,<ns>.",
  "references": [
    {
      "title": "Nmap Firewall/IDS Evasion",
      "url": "https://nmap.org/book/man-bypass-firewalls-ids.html"
    },
    {
      "title": "HTB Academy - Network Enumeration with Nmap",
      "url": "https://academy.hackthebox.com/module/details/19"
    },
    {
      "title": "OffSec PEN-200 (OSCP)",
      "url": "https://www.offsec.com/courses/pen-200/"
    }
  ],
  "recommended": [
    {
      "id": "nmap-firewall-scan-types",
      "rel": "next",
      "note": "Map the ruleset once through."
    }
  ],
  "id": "nmap-source-port",
  "name": "Evasion - Trusted Source Port (53)",
  "command": "sudo nmap <ip> -p50000 -sS -Pn -n --disable-arp-ping --packet-trace --source-port 53",
  "description": "Firewalls often trust traffic from port 53 (DNS). Scanning from --source-port 53 can slip past a filtered port; netcat can then connect from the same source port.",
  "opsec": "moderate",
  "mitre": [
    "T1046"
  ],
  "exam": "exam-ok",
  "defense": {
    "why_it_works": "Some firewalls and IDS/IPS systems are configured to trust traffic from TCP port 53 (DNS). Using --source-port 53 exploits this trust rule, causing packets to be passed through firewall rules that would otherwise block them.",
    "prerequisites": "Network access and a firewall that trusts a specific source port (e.g. 53/DNS).",
    "impact": "Bypass of source-port-based ACLs, reaching ports that would otherwise be filtered.",
    "detection": "[MITRE T1046 AN1583] Monitor for execution of network enumeration utilities (nmap, netcat, net.exe, ping.exe, tracert.exe) in short succession, particularly when chained with lateral movement tools or system enumeration commands. [AN1584] Sequential ping, arp, or traceroute commands issued from bash scripts or interactive shells to map remote hosts are a strong behavioral indicator.",
    "artifacts": "Traffic sourced from port 53 (or another trusted port) toward unusual destination ports in firewall logs.",
    "prevention": "Do not create firewall allow rules based on source port alone. Configure IDS/IPS to properly inspect all traffic including packets sourced from port 53.\n\n[MITRE T1046 M1042] Disable or remove unnecessary services and functionality that expose attack surface. [M1030] Implement network segmentation to limit the set of hosts reachable from any given compromised system. Use firewalls to restrict which source addresses can reach each service port.",
    "evasion": "Setting a trusted source port (--source-port 53/80/443) defeats firewalls that allow traffic from DNS/HTTP/HTTPS by port; pair with slow timing and fragmentation for stealthier probing.",
    "sources": [
      "HTB M03",
      "MITRE T1046"
    ],
    "misconfiguration": "Network perimeters that do not filter ICMP, do not use egress filtering, or expose unnecessary services (all ports open) allow comprehensive port scanning. Systems without host-based firewalls or IDS/IPS allow scan traffic to enumerate every open service, revealing attack surface. No rate-limiting on inbound probes means stealth scans complete quickly.",
    "vulnerable_config": "# No firewall between attacker and target (or overly permissive rules):\n# iptables -L  ->  Chain INPUT (policy ACCEPT)  [no rules]\n# Or: Windows Firewall disabled\n# Every service's port is reachable from any source\n\n# No IDS/IPS:\n# Snort/Suricata not deployed; no AWS GuardDuty / Azure Defender\n# Nmap SYN scan completes in seconds with no alert generated",
    "secure_config": "# Linux host-based firewall — default-deny inbound:\niptables -P INPUT DROP\niptables -P FORWARD DROP\niptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT\niptables -A INPUT -i lo -j ACCEPT\n# Only open required services:\niptables -A INPUT -p tcp --dport 443 -j ACCEPT  # HTTPS only\niptables -A INPUT -p tcp --dport 22 -s 10.10.1.0/24 -j ACCEPT  # SSH from mgmt network\n\n# Rate-limit scan detection (drops SYN floods):\niptables -A INPUT -p tcp --syn -m limit --limit 1/s --limit-burst 3 -j ACCEPT\niptables -A INPUT -p tcp --syn -j DROP\n\n# Deploy IDS/IPS:\n# Snort/Suricata rules detect nmap OS detection probes\n# AWS: GuardDuty (detects port scans from EC2)\n# Cloud: Security Groups default-deny with explicit allow rules"
  }
}
