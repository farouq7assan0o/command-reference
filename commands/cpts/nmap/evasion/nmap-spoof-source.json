{
  "type": "command",
  "platform": "linux",
  "requires": [
    "network-access"
  ],
  "protocols": [
    "tcp"
  ],
  "tools": [
    "nmap"
  ],
  "tags": [
    "nmap",
    "spoof",
    "os-detection",
    "evasion"
  ],
  "category": "Enumeration",
  "subcategory": "Nmap - Firewall & IDS/IPS Evasion",
  "certifications": [
    "CPTS",
    "OSCP"
  ],
  "primary_cert": "CPTS",
  "source": "CPTS Module 03: Network Enumeration with Nmap",
  "examples": [
    {
      "label": "Spoof source IP over an interface",
      "command": "sudo nmap 10.129.2.28 -n -Pn -p 445 -O -S 10.129.2.200 -e tun0"
    },
    {
      "label": "Lab example",
      "command": "sudo nmap 10.129.2.28 -n -Pn -p445 -O"
    }
  ],
  "notes": "`-S <ip>` spoofs the source address (needs `-e <iface>` and a way to see replies - usually same subnet). Combine with decoys. Mostly useful to test IDS/egress rules, not to get results back.",
  "references": [
    {
      "title": "Nmap OS Detection",
      "url": "https://nmap.org/book/osdetect.html"
    },
    {
      "title": "HTB Academy - Network Enumeration with Nmap",
      "url": "https://academy.hackthebox.com/module/details/19"
    },
    {
      "title": "OffSec PEN-200 (OSCP)",
      "url": "https://www.offsec.com/courses/pen-200/"
    }
  ],
  "recommended": [
    {
      "id": "nmap-decoy-scan",
      "rel": "alternative",
      "note": "Decoys are the more practical evasion."
    },
    {
      "id": "nmap-firewall-scan-types",
      "rel": "prereq",
      "note": "Identify the firewall/IDS you need to evade first"
    }
  ],
  "id": "nmap-spoof-source",
  "name": "Evasion - OS Detection & Source Spoofing",
  "command": "sudo nmap <ip> -n -Pn -p445 -O",
  "description": "Test a firewall rule by fingerprinting the OS (-O), and spoof your source IP (-S) over a chosen interface (-e) - useful when the firewall trusts a specific address.",
  "opsec": "moderate",
  "mitre": [
    "T1046"
  ],
  "exam": "exam-ok",
  "defense": {
    "why_it_works": "Firewall rules may restrict access by source subnet, allowing only specific IP ranges to reach certain services. Spoofing the source IP with -S allows testing whether a different subnet has access that the attacker's real IP does not.",
    "prerequisites": "A network position where the spoofed source subnet is trusted and replies are routable/observable.",
    "impact": "Access to services restricted to a trusted source range by forging the source IP.",
    "detection": "Spoofed packets are often filtered out by ISPs and routers even within the same network range. Asymmetric routing (responses going to a spoofed IP the attacker cannot receive) limits the technique's usefulness.\n\n[MITRE T1046 AN1583] Monitor for execution of network enumeration utilities (nmap, netcat, net.exe, ping.exe, tracert.exe) in short succession, particularly when chained with lateral movement tools or system enumeration commands. [AN1584] Sequential ping, arp, or traceroute commands issued from bash scripts or interactive shells to map remote hosts are a strong behavioral indicator.",
    "artifacts": "Spoofed-source packets; replies routed to the spoofed address (visible if the attacker can sniff that segment).",
    "prevention": "[MITRE T1046 M1042] Disable or remove unnecessary services and functionality that expose attack surface. [M1030] Implement network segmentation to limit the set of hosts reachable from any given compromised system. Use firewalls to restrict which source addresses can reach each service port.",
    "evasion": "Source-IP spoofing (-S) only works when the spoofed address is trusted by the target's rules AND you can observe the replies (same segment or a sniffing position); pair it with decoys (-D) and slow timing so the real source stays hidden. Covered in the module's Firewall and IDS Evasion section.",
    "sources": [
      "HTB M03",
      "MITRE T1046"
    ],
    "misconfiguration": "Network perimeters that do not filter ICMP, do not use egress filtering, or expose unnecessary services (all ports open) allow comprehensive port scanning. Systems without host-based firewalls or IDS/IPS allow scan traffic to enumerate every open service, revealing attack surface. No rate-limiting on inbound probes means stealth scans complete quickly.",
    "vulnerable_config": "# No firewall between attacker and target (or overly permissive rules):\n# iptables -L  ->  Chain INPUT (policy ACCEPT)  [no rules]\n# Or: Windows Firewall disabled\n# Every service's port is reachable from any source\n\n# No IDS/IPS:\n# Snort/Suricata not deployed; no AWS GuardDuty / Azure Defender\n# Nmap SYN scan completes in seconds with no alert generated",
    "secure_config": "# Linux host-based firewall — default-deny inbound:\niptables -P INPUT DROP\niptables -P FORWARD DROP\niptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT\niptables -A INPUT -i lo -j ACCEPT\n# Only open required services:\niptables -A INPUT -p tcp --dport 443 -j ACCEPT  # HTTPS only\niptables -A INPUT -p tcp --dport 22 -s 10.10.1.0/24 -j ACCEPT  # SSH from mgmt network\n\n# Rate-limit scan detection (drops SYN floods):\niptables -A INPUT -p tcp --syn -m limit --limit 1/s --limit-burst 3 -j ACCEPT\niptables -A INPUT -p tcp --syn -j DROP\n\n# Deploy IDS/IPS:\n# Snort/Suricata rules detect nmap OS detection probes\n# AWS: GuardDuty (detects port scans from EC2)\n# Cloud: Security Groups default-deny with explicit allow rules"
  }
}
