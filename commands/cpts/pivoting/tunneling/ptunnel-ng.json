{
  "id": "ptunnel-ng",
  "name": "Ptunnel-ng - ICMP Tunneling",
  "command": "sudo ./ptunnel-ng -r<pivot_ip> -R22",
  "description": "Tunnels TCP (typically SSH) inside ICMP echo packets, useful when only ping is allowed out. Run the server on the pivot and the client on the attack host, then SSH to the local tunnel port and add -D for a SOCKS proxy over the ICMP channel.",
  "platform": "linux",
  "requires": [
    "shell"
  ],
  "protocols": [
    "tcp"
  ],
  "tools": [
    "ptunnel-ng",
    "ssh"
  ],
  "tags": [
    "ptunnel-ng",
    "icmp",
    "tunnel",
    "pivoting"
  ],
  "category": "Pivoting & Tunneling",
  "subcategory": "ICMP Tunneling",
  "certifications": [
    "CPTS",
    "OSCP"
  ],
  "primary_cert": "CPTS",
  "source": "CPTS Module 12: Pivoting, Tunneling, and Port Forwarding",
  "examples": [
    {
      "label": "ptunnel-ng",
      "command": "sudo ./ptunnel-ng -r10.129.202.64 -R22"
    },
    {
      "label": "ptunnel-ng",
      "command": "sudo ./ptunnel-ng -p10.129.202.64 -l2222 -r10.129.202.64 -R22"
    },
    {
      "label": "Dynamic SOCKS (-D)",
      "command": "ssh -D 9050 -p2222 -lubuntu 127.0.0.1"
    }
  ],
  "notes": "Server on pivot: -r<pivot_ip> -R22. Client on attacker: -p<pivot_ip> -l2222. Then SSH to 127.0.0.1:2222, optionally with -D for SOCKS.",
  "references": [
    {
      "title": "HTB Academy - Pivoting, Tunneling, and Port Forwarding",
      "url": "https://academy.hackthebox.com/module/details/158"
    },
    {
      "title": "ptunnel-ng",
      "url": "https://github.com/utoni/ptunnel-ng"
    },
    {
      "title": "OffSec PEN-200 (OSCP)",
      "url": "https://www.offsec.com/courses/pen-200/"
    }
  ],
  "recommended": [
    {
      "id": "proxychains-run",
      "note": "Route tooling over the ICMP tunnel.",
      "rel": "next"
    }
  ],
  "opsec": "quiet",
  "mitre": [
    "T1572"
  ],
  "steps": [
    {
      "label": "Copy ptunnel-ng to the pivot",
      "command": "scp -r ptunnel-ng <user>@<pivot_ip>:~/"
    },
    {
      "label": "Pivot: start the ICMP tunnel server",
      "command": "sudo ./ptunnel-ng -r<pivot_ip> -R22"
    },
    {
      "label": "Attacker: start the client (forward local 2222 -> pivot:22 over ICMP)",
      "command": "sudo ./ptunnel-ng -p<pivot_ip> -l2222 -r<pivot_ip> -R22"
    },
    {
      "label": "SSH through the tunnel",
      "command": "ssh -p2222 <user>@127.0.0.1"
    }
  ],
  "exam": "exam-ok",
  "defense": {
    "why_it_works": "ptunnel-ng tunnels TCP inside ICMP echo request/reply packets, smuggling connections through firewalls that permit ping but block other egress.",
    "prerequisites": "A ptunnel-ng client on the target and ICMP allowed outbound to the attacker's proxy.",
    "impact": "TCP connectivity (e.g. SSH, a SOCKS relay) tunnelled over ICMP where other protocols are blocked.",
    "detection": "[MITRE T1572] ICMP echo with large or patterned payloads and abnormal request/reply volume - normal pings are small and infrequent; payload inspection reveals encapsulated TCP.",
    "artifacts": "Oversized/high-volume ICMP flows to the attacker; the ptunnel-ng process.",
    "prevention": "[MITRE T1572 M1037] block or rate-limit outbound ICMP and inspect payloads; [M1031] IDS on ICMP tunneling patterns (payload size/entropy).",
    "evasion": "Small, paced ICMP payloads mimicking normal ping reduce detection; blend with expected monitoring-ping traffic.",
    "sources": [
      "HTB M12",
      "MITRE T1572"
    ],
    "misconfiguration": "The network lacks proper segmentation — a compromised DMZ host has direct layer-3 reachability to internal infrastructure (database servers, DCs, internal admin panels). No egress filtering blocks outbound HTTP/HTTPS connections to arbitrary hosts, allowing C2 channels and reverse tunnels. Internal firewalls only filter inbound traffic, not east-west or outbound.",
    "vulnerable_config": "# Network design: flat Layer-3 network, all VLANs can reach each other\n# DMZ web server can ping/connect to: 10.10.10.0/24 (internal LAN), 10.10.20.0/24 (DB subnet)\n# No egress proxy — direct outbound HTTP to any IP allowed\n\n# iptables on pivot host (none blocking outbound):\niptables -L OUTPUT  # policy ACCEPT, no rules\n\n# This allows: chisel client 10.10.10.5:8080 R:socks\n# Attacker now has SOCKS5 access to the entire internal network",
    "secure_config": "# Network segmentation with explicit allow rules:\n# DMZ -> Internet: only specific egress IPs (patch servers, DNS)\n# DMZ -> Internal: ONLY the DB ports this specific app needs\n# Internal -> Internal: segment by tier (web tier can't reach DC directly)\n\n# Egress filtering — block all outbound except known-good:\n# FortiGate / Palo Alto / iptables:\niptables -P OUTPUT DROP\niptables -A OUTPUT -d 8.8.8.8 -p udp --dport 53 -j ACCEPT  # DNS to specific server\niptables -A OUTPUT -p tcp --dport 443 -d <known_update_servers> -j ACCEPT\niptables -A OUTPUT -m state --state ESTABLISHED,RELATED -j ACCEPT\n\n# IDS/IPS signatures for Chisel, ngrok, frp traffic patterns\n# DNS monitoring — detect DNS tunneling (dnscat2) via unusual query frequency/length\n# HTTP proxy with TLS inspection — detect reverse tunnels in HTTPS streams"
  }
}
